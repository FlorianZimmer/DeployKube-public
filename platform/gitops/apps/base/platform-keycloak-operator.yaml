apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: platform-keycloak-operator
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "0.5"
spec:
  project: platform
  destination:
    name: in-cluster
    namespace: keycloak-operator
  source:
    repoURL: https://forgejo-https.forgejo.svc.cluster.local/platform/cluster-config.git
    targetRevision: main
    path: components/platform/keycloak/operator
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ApplyOutOfSyncOnly=true
      - ServerSideApply=true

apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: data-postgres-powerdns-smoke-tests
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "15"
spec:
  project: platform
  destination:
    name: in-cluster
    namespace: dns-system
  source:
    repoURL: https://forgejo-https.forgejo.svc.cluster.local/platform/cluster-config.git
    targetRevision: main
    path: components/data/postgres/powerdns/smoke-tests/base
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - PrunePropagationPolicy=foreground
      - ServerSideApply=true


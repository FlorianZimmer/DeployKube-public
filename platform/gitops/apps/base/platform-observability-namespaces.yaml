apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: platform-observability-namespaces
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "0.5"
spec:
  project: platform
  destination:
    name: in-cluster
    namespace: argocd
  source:
    repoURL: https://forgejo-https.forgejo.svc.cluster.local/platform/cluster-config.git
    targetRevision: main
    path: components/platform/observability/namespaces
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ApplyOutOfSyncOnly=true

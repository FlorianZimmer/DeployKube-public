apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: networking-dns-powerdns
  namespace: argocd
spec:
  syncPolicy:
    retry:
      limit: 10
      backoff:
        duration: 10s
        factor: 2
        maxDuration: 5m


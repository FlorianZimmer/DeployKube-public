apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: backup-restore-canary-data
  labels:
    app.kubernetes.io/name: backup-system
    app.kubernetes.io/component: restore-canary
    darksite.cloud/backup: restic
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: shared-rwo

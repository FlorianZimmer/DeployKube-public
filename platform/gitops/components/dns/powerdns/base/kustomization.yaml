apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: dns-system
resources:
  - externaldns-crd-dnsendpoint.yaml
  - externalsecrets.yaml
  - destinationrule-powerdns-api.yaml
  - powerdns-config.yaml
  - powerdns-deployment.yaml
  - externaldns.yaml
  - powerdns-db-init-job.yaml
  - zone-bootstrap-job.yaml

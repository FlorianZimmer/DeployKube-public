# overlays/

This directory is intentionally kept in Git (via this README) to ensure clean
checkouts satisfy the DNS wiring controller-cutover validation.

Do not add `*.yaml` renderer artifacts back here.

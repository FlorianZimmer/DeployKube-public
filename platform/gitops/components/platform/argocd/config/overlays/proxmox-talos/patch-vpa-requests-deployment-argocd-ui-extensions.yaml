apiVersion: apps/v1
kind: Deployment
metadata:
  name: argocd-ui-extensions
  namespace: argocd
spec:
  template:
    spec:
      containers:
        - name: nginx
          resources:
            requests:
              memory: "64Mi"

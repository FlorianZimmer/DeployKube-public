apiVersion: apps/v1
kind: Deployment
metadata:
  name: mimir-gateway
  namespace: mimir
spec:
  template:
    spec:
      containers:
        - name: gateway
          resources:
            requests:
              cpu: "25m"
              memory: "64Mi"
            limits:
              memory: "96Mi"

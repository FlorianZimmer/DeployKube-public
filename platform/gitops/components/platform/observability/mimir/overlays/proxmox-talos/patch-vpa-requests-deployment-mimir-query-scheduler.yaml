apiVersion: apps/v1
kind: Deployment
metadata:
  name: mimir-query-scheduler
  namespace: mimir
spec:
  template:
    spec:
      containers:
        - name: query-scheduler
          resources:
            requests:
              cpu: "25m"
              memory: "64Mi"
            limits:
              memory: "96Mi"

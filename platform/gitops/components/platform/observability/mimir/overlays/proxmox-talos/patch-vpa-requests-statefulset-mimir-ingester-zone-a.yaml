apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: mimir-ingester-zone-a
  namespace: mimir
spec:
  template:
    spec:
      containers:
        - name: ingester
          resources:
            requests:
              cpu: "100m"
              memory: "1584Mi"
            limits:
              memory: "2376Mi"

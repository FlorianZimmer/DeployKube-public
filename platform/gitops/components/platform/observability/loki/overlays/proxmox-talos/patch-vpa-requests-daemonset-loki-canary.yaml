apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: loki-canary
  namespace: loki
spec:
  template:
    spec:
      containers:
        - name: loki-canary
          resources:
            requests:
              cpu: "25m"
              memory: "64Mi"
            limits:
              memory: "96Mi"

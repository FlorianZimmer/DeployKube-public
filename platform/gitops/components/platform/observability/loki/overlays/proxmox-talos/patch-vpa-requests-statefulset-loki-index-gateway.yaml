apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: loki-index-gateway
  namespace: loki
spec:
  template:
    spec:
      containers:
        - name: index-gateway
          resources:
            requests:
              cpu: "15m"
              memory: "64Mi"
            limits:
              memory: "96Mi"

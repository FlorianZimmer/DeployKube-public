apiVersion: apps/v1
kind: Deployment
metadata:
  name: loki-querier
  namespace: loki
spec:
  template:
    spec:
      containers:
        - name: querier
          resources:
            requests:
              cpu: "25m"
              memory: "112Mi"
            limits:
              memory: "168Mi"

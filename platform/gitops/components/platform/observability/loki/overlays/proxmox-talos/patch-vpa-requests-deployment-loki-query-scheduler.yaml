apiVersion: apps/v1
kind: Deployment
metadata:
  name: loki-query-scheduler
  namespace: loki
spec:
  template:
    spec:
      containers:
        - name: query-scheduler
          resources:
            requests:
              cpu: "15m"
              memory: "64Mi"
            limits:
              memory: "96Mi"

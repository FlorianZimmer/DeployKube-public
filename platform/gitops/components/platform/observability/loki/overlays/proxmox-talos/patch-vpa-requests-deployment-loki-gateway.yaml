apiVersion: apps/v1
kind: Deployment
metadata:
  name: loki-gateway
  namespace: loki
spec:
  template:
    spec:
      containers:
        - name: nginx
          resources:
            requests:
              cpu: "15m"
              memory: "64Mi"
            limits:
              memory: "96Mi"

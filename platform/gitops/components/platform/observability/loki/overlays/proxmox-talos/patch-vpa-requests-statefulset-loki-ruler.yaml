apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: loki-ruler
  namespace: loki
spec:
  template:
    spec:
      containers:
        - name: ruler
          resources:
            requests:
              cpu: "15m"
              memory: "112Mi"
            limits:
              memory: "168Mi"

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Grafana dashboards provisioned as ConfigMaps
# These are mounted by the Grafana chart via dashboardsConfigMaps.
# See docs/design/observability-lgtm-troubleshooting-alerting.md for dashboard conventions.

resources:
  - configmap-platform-dashboards.yaml

apiVersion: v1
kind: Service
metadata:
  name: platform-ntp
  namespace: time-system
  labels:
    app.kubernetes.io/name: platform-ntp
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: platform-ntp
  ports:
    - name: ntp
      protocol: UDP
      port: 123
      targetPort: ntp

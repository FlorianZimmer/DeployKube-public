apiVersion: apps/v1
kind: Deployment
metadata:
  name: keycloak-operator
spec:
  template:
    spec:
      containers:
        - name: keycloak-operator
          resources:
            requests:
              cpu: "15m"
              memory: "400Mi"
            limits:
              memory: "600Mi"

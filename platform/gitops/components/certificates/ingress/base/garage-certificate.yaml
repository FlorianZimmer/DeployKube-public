apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: garage-tls
spec:
  secretName: garage-tls
  dnsNames:
    - garage.placeholder.invalid
  issuerRef:
    name: step-ca
    kind: ClusterIssuer
  privateKey:
    algorithm: RSA
    size: 2048

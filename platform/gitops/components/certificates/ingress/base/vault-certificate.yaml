apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: vault-tls
spec:
  secretName: vault-tls
  dnsNames:
    - vault.placeholder.invalid
  issuerRef:
    name: step-ca
    kind: ClusterIssuer
  privateKey:
    algorithm: RSA
    size: 2048

apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: kiali-tls
spec:
  secretName: kiali-tls
  dnsNames:
    - kiali.placeholder.invalid
  issuerRef:
    name: step-ca
    kind: ClusterIssuer
  privateKey:
    algorithm: RSA
    size: 2048

apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: step-ca-step-certificates
  namespace: step-system
spec:
  template:
    spec:
      containers:
        - name: step-certificates
          resources:
            requests:
              cpu: "15m"
              memory: "64Mi"
            limits:
              memory: "96Mi"

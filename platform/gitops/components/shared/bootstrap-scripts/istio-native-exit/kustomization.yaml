apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

configMapGenerator:
  - name: istio-native-exit-script
    files:
      - istio-native-exit.sh

generatorOptions:
  disableNameSuffixHash: true

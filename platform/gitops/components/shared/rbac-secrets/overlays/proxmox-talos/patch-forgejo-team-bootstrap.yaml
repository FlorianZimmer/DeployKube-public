apiVersion: batch/v1
kind: Job
metadata:
  name: forgejo-team-bootstrap
  namespace: rbac-system
spec:
  template:
    spec:
      containers:
        - name: bootstrap
          env:
            - name: KEYCLOAK_BASE_URL
              value: "https://keycloak.prod.internal.example.com:8443"

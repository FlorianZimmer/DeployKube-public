apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

configMapGenerator:
  - name: secrets-bootstrap-script
    files:
      - bootstrap.sh=scripts/bootstrap.sh

generatorOptions:
  disableNameSuffixHash: true

resources:
  - rbac.yaml
  - job.yaml

apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: keycloak-upstream-ldap
  namespace: keycloak
spec:
  refreshInterval: 1m
  secretStoreRef:
    kind: ClusterSecretStore
    name: vault-core
  target:
    name: keycloak-upstream-ldap
    creationPolicy: Owner
    template:
      type: Opaque
  dataFrom:
    - extract:
        key: keycloak/upstream-ldap

apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: keycloak-upstream-saml
  namespace: keycloak
spec:
  refreshInterval: 1m
  secretStoreRef:
    kind: ClusterSecretStore
    name: vault-core
  target:
    name: keycloak-upstream-saml
    creationPolicy: Owner
    template:
      type: Opaque
  dataFrom:
    - extract:
        key: keycloak/upstream-saml

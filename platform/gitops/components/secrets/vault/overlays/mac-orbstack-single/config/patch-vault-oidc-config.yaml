apiVersion: batch/v1
kind: CronJob
metadata:
  name: vault-oidc-config
  namespace: vault-system
spec:
  jobTemplate:
    spec:
      template:
        spec:
          containers:
            - name: oidc-config
              env:
                - name: KEYCLOAK_OIDC_HOST
                  value: keycloak.dev-single.internal.example.com
                - name: KEYCLOAK_OIDC_SCHEME
                  value: https
                - name: VAULT_OIDC_HOST
                  value: vault.dev-single.internal.example.com


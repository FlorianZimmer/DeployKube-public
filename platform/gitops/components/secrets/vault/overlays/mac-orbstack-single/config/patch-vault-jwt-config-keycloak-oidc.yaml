apiVersion: batch/v1
kind: CronJob
metadata:
  name: vault-jwt-config
  namespace: vault-system
spec:
  jobTemplate:
    spec:
      template:
        spec:
          containers:
            - name: jwt-config
              env:
                - name: KEYCLOAK_OIDC_HOST
                  value: keycloak.dev-single.internal.example.com
                - name: KEYCLOAK_OIDC_SCHEME
                  value: https


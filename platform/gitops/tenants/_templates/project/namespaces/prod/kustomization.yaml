apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace-app.yaml
  # Optional: support sessions (platform-owned; time-bound)
  # - ../../../../support-sessions/<sessionId>
  # Optional: tenant-facing S3 primitive (platform-owned)
  # - configmap-garage-tenant-s3-intent-app.yaml
  # - networkpolicy-allow-garage-s3-egress.yaml
  # - externalsecret-tenant-s3-app.yaml

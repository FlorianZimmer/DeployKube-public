apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: egress-all-namespaces
  namespace: t-acme-p-payments-dev-app
spec:
  podSelector: {}
  policyTypes:
    - Egress
  egress:
    - to:
        - namespaceSelector: {}
      ports:
        - protocol: TCP
          port: 443

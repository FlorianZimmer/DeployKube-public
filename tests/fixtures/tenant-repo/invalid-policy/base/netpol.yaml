apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: egress-anywhere
  namespace: t-acme-p-payments-dev-app
spec:
  podSelector: {}
  policyTypes:
    - Egress
  egress:
    - to:
        - ipBlock:
            cidr: 0.0.0.0/0

